import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-tutorial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
